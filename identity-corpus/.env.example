OPENAI_API_KEY=sk-...

# --- sheets push (GS staging tab) ---
# Service account JSON. The SA email must be shared (Editor) on the target sheet.
GCP_SERVICE_ACCOUNT_FILE=../util-sheetprocessing/credentials/service_account.json
# GS document name (title, not URL) of the English-training sheet.
ENGLISH_SHEET_NAME=
# Staging tab name; raw output lands here for operator review before going live.
STAGING_WORKSHEET=staging
